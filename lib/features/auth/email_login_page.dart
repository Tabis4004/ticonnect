/// Cet écran a fusionné avec `SignInPage` dans `auth_pages.dart`.
///
/// La connexion accepte désormais indifféremment un pseudo ou une adresse
/// email : les comptes administrateur et de test passent par le même
/// formulaire que les utilisateurs. Fichier conservé vide pour ne pas casser
/// un import oublié ; supprimable sans risque.
library;
